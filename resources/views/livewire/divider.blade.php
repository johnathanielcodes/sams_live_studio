@props(['title', 'id'])
<span class="flex py-8 items-center" id="{{ $id }}">
    <span class="h-px flex-1 bg-gray-300"></span>

    <h2 class="shrink-0 px-4 h2 dark:text-white text-gray-900">{{ $title }}</h2>

    <span class="h-px flex-1 bg-gray-300"></span>
</span>
